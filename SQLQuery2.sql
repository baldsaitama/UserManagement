Drop table Roles;
Create table pl_roles(role_id int primary key identity, name varchar(50));

Create table pl_departments(dept_id int primary key identity, dept_name varchar(50), description nvarchar(100));


